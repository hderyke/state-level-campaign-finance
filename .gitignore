
#setup stuff
__pycache__/
*.pyc
*.db
.DS_Store
.tmp/
.idea/
.venv/
venv/

#local run data (recreated automatically by the pipeline — see repo_reorg notes)
data/
logs/
metadata/

# personal AWS deployment + operational tooling — not part of the public
# pipeline; requires your own AWS account/bucket/Lambda/ECS setup to use
cloud/
client-facing/
src/daemon.py
src/emailer.py

# credentials — never commit
.env

# personal AI-agent workflow notes — not needed to use the pipeline
CLAUDE.md

